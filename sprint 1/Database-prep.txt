I am responsible for:
-Create tables for the database
-Put dummy data into the tables
-Write queries for the features

setup steps
I need to create:
1. A user table which holds the information for the account
	(Pseudocode)Create table user(PRIMARY Varchar UserID, Varchar UserEmail, Varchar username, Varchar Password_Hash, Date Created_at, Date Updated_at)

2. The Poll table and subsequent Poll Option Table
	(Pseudocode)Create table Poll(PRIMARY Varchar PollID, Varchar Question, Date CreatedAt, Foreign Varchar UserID)
	(Pseudocode)Create table PollOption(PRIMARY Varchar OptionID, Varchar OptionText, Foreign Varchar PollID)

3.Forum Tables(Post and Comment)
	(Pseudocode)Create table Post(PRIMARY Varchar PostID, Varchar Body, Varchar Title, Date CreatedAt, Foreign Varchar UserID)
	(Pseudocode)Create table Comment(Primary Varchar CommentID, Varchat CommentText, Date CreatedAt, Foreign Varchar UserID, Foreign Varchar PostID)

4. Add Dummy Data into tables
	(Pseudocode) Add INTO User Dummyuser VALUES(UserID, UserEmail, Username, Hashed_Password, CreatedAt, UpdatedAT) (Varchar U01, Dummy@hotmail.com, Dummyuser, Hashed Password, 02/10/2026, 02/10/2026)

5. Have queries

Development plan:
1. Work on the tables-stores the needed information. Begin with User, then go to ratings, move onto option, and then forums

2. For this step, "Done" would be the tables completed

3. Add a dummy user

4. Have this dummy user make a post body

5. Make a comment derived from the post

6. Make a dummy rating for a product

7. Make a poll with dummy option

8. Have dummy options for the poll

"Done" for this would be every table having at least one entity as a barebones example to make sure it works

9. Begin queries for the user

10. Work on queries for the ratings

11. Go onto queries for Poll

12. Make queries for the option

13. Get to queries for the Post 

14. Finish with queries for the comment

Risks/Questions:
I may need to get a better look into the features to understand if I need to have more Tables for data(maybe I need to have a product to attach ratings to?)
Likewise, I will need to see what the features need from the tables to do the queries